{{- if and (not .Values.hermes.apiKey.existingSecret) .Values.hermes.apiKey.externalSecret.enabled -}}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "voice-assistant.hermesSecretName" . }}
  labels:
    {{- include "voice-assistant.labels" . | nindent 4 }}
spec:
  secretStoreRef:
    name: {{ .Values.hermes.apiKey.externalSecret.store }}
    kind: {{ .Values.hermes.apiKey.externalSecret.storeKind }}
  refreshInterval: {{ .Values.hermes.apiKey.externalSecret.refreshInterval | quote }}
  target:
    name: {{ include "voice-assistant.hermesSecretName" . }}
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: {{ .Values.hermes.apiKey.secretKey }}
      remoteRef:
        key: {{ .Values.hermes.apiKey.externalSecret.remoteKey }}
        property: {{ .Values.hermes.apiKey.externalSecret.property }}
{{- else if and (not .Values.hermes.apiKey.existingSecret) .Values.hermes.apiKey.value -}}
# Inline API key — INSECURE. Prefer hermes.apiKey.externalSecret.enabled=true.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "voice-assistant.hermesSecretName" . }}
  labels:
    {{- include "voice-assistant.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.hermes.apiKey.secretKey }}: {{ .Values.hermes.apiKey.value | quote }}
{{- end -}}
